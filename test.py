import copy

import numpy as np
import math

if __name__ == '__main__':
    a = math.cos(120/180*math.pi)
    print(a)

    # 扫描点预处理
    # d = 0
    # per_scan = points_origin[0, 1]
    # self.num_of_points_per_scan = int(per_scan)
    # points = copy.deepcopy(points_origin)
    #
    # while d < points_origin.shape[0]:
    #     points = np.delete(points, int(d), axis=0)
    #     d += per_scan

    # a = 1234
    # b = int(a / 100) * 100
    # print(b)

    # a = []
    # a.append([1, 2])
    # a.append([3, 4])
    # print(a)

